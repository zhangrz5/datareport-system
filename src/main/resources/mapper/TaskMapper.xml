<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.datareport.mapper.TaskMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.company.datareport.entity.Task">
        <id column="id" property="id" />
        <result column="task_code" property="taskCode" />
        <result column="task_name" property="taskName" />
        <result column="task_type" property="taskType" />
        <result column="data_source" property="dataSource" />
        <result column="task_desc" property="taskDesc" />
        <result column="task_status" property="taskStatus" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="deadline" property="deadline" />
        <result column="collect_rule" property="collectRule" />
        <result column="collect_sql" property="collectSql" />
        <result column="target_datasource" property="targetDatasource" />
        <result column="retry_count" property="retryCount" />
        <result column="error_msg" property="errorMsg" />
        <result column="execute_result" property="executeResult" />
        <result column="remark" property="remark" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <!-- 查询待处理的任务列表 -->
    <select id="selectPendingTasks" resultMap="BaseResultMap">
        SELECT * FROM t_task
        WHERE deleted = 0
        AND task_status = 0
        AND (deadline IS NULL OR deadline > NOW())
        ORDER BY create_time ASC
    </select>

    <!-- 根据任务编号查询任务 -->
    <select id="selectByTaskCode" resultMap="BaseResultMap">
        SELECT * FROM t_task
        WHERE deleted = 0
        AND task_code = #{taskCode}
        LIMIT 1
    </select>

    <!-- 更新任务状态 -->
    <update id="updateTaskStatus">
        UPDATE t_task
        SET task_status = #{status},
            update_time = NOW()
        WHERE id = #{taskId}
        AND deleted = 0
    </update>

</mapper>
