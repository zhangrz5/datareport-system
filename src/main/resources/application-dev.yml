spring:
  # 多数据源配置
  datasource:
    dynamic:
      # 设置默认的数据源或者数据源组
      primary: master
      # 严格匹配数据源,默认false. true未匹配到指定数据源时抛异常,false使用默认数据源
      strict: false
      datasource:
        # 主数据源(达梦数据库)
        master:
          driver-class-name: dm.jdbc.driver.DmDriver
          url: jdbc:dm://100.100.200.213:5236/DATAREPORT?characterEncoding=UTF-8
          username: SYSDBA
          password: Ec123456
          type: com.alibaba.druid.pool.DruidDataSource
          druid:
            initial-size: 5
            min-idle: 5
            max-active: 20
            max-wait: 60000
            time-between-eviction-runs-millis: 60000
            min-evictable-idle-time-millis: 300000
            validation-query: SELECT 1 FROM DUAL
            test-while-idle: true
            test-on-borrow: false
            test-on-return: false
            pool-prepared-statements: true
            max-pool-prepared-statement-per-connection-size: 20

        # 从数据源1(SQLite 密钥数据库)
        slave1:
          driver-class-name: org.sqlite.JDBC
          url: jdbc:sqlite:E:/project/国企上报/0025_1001_20251121.db
          # SQLite 不需要用户名密码
          username:
          password:
          type: com.alibaba.druid.pool.DruidDataSource
          druid:
            initial-size: 1
            min-idle: 1
            max-active: 5
            max-wait: 60000
            # SQLite 不支持标准的 validation query，使用 SELECT 1
            validation-query: SELECT 1
            test-while-idle: true
            test-on-borrow: false
            test-on-return: false
#
#        # 从数据源2(外部系统数据库 - PostgreSQL示例)
#        slave2:
#          driver-class-name: org.postgresql.Driver
#          url: jdbc:postgresql://localhost:5432/external_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
#          username: postgres
#          password: postgres
#          type: com.alibaba.druid.pool.DruidDataSource
#          druid:
#            initial-size: 5
#            min-idle: 5
#            max-active: 20
  
  # Redis 配置 (可选 - 用于分布式锁)
  # 如果不配置Redis,系统将以单机模式运行,定时任务不使用分布式锁
  # 如需启用分布式锁功能,请取消以下注释并配置正确的Redis连接信息
  #  redis:
  #    host: 192.168.123.1
  #    port: 6379
  #    password:
  #    database: 0
  #    timeout: 5000ms
  #    lettuce:
  #      pool:
  #        max-active: 20
  #        max-wait: -1ms
  #        max-idle: 10
  #        min-idle: 0

  # 单机环境如果不使用Redis,请取消注释以下配置以禁用Redis自动配置
  # autoconfigure:
  #   exclude:
  #     - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
  #     - org.redisson.spring.starter.RedissonAutoConfiguration

# MyBatis-Plus SQL日志开关（已在 application.yml 中配置）
#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    root: INFO
    com.company.datareport: INFO
    com.baomidou.dynamic: INFO

# 应用自定义配置
app:
  # 监管平台配置
  platform:
    # 获取任务列表API
    task-list-url: http://platform.example.com/api/tasks
    # 上报数据API
    upload-url: http://platform.example.com/api/upload
    # 企业编码
    enterprise-code: ENTERPRISE_001
    # 认证密钥
    auth-key: your-auth-key
    # 请求超时时间(秒)
    timeout: 30
    #sm2密钥
    sm2-key:
      152E1DD4EBB0978B62CCEF3F16C47BB31A8455EAD47FADE3B122A2F15EDB346B4614A3B442B607B0A2F296113AB0CD97ABE99049990E9B0E61F33DC4BC735909
    #sm4密钥
    sm4-key:
      5b2ee70a2cd34d109ba1fb0a03bd6971

  # 任务调度配置
  scheduler:
    # 是否启用定时任务
    enabled: true
    # 数据上报Cron表达式(每天凌晨3点执行)
    data-upload-cron: 0 0 4 * * ?

  # 重试配置
  retry:
    # 最大重试次数
    max-attempts: 3
    # 重试间隔(毫秒)
    backoff: 2000

  # 文件上传配置
  upload:
    # 上传文件存储路径
    upload-path: E:/project/国企上报/upload/
    # 文件上传临时目录
    temp-path: E:/project/国企上报/temp/
    # 单文件最大大小(MB)
    max-file-size: 500

  # SQLite 数据库文件配置
  sqlite:
    # SQLite 数据库文件路径
    file-path: E:/project/国企上报/0025_1001_20251121.db

  #上报企业名称和编码
  corporate:
    name: 南京市雨花台区人民政府国有资产监督管理办公室
    code: 11320114745362524W





