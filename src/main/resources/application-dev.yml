spring:
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:postgresql://localhost:5432/datareport_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
          username: postgres
          password: postgres
        slave1:
          url: jdbc:mysql://localhost:3306/enterprise_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
          username: root
          password: root
        slave2:
          url: jdbc:postgresql://localhost:5432/external_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
          username: postgres
          password: postgres
  
  redis:
    host: localhost
    port: 6379
    password:
    database: 0

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    root: INFO
    com.company.datareport: DEBUG
    com.baomidou.dynamic: DEBUG

app:
  platform:
    task-list-url: http://dev-platform.example.com/api/tasks
    upload-url: http://dev-platform.example.com/api/upload
    enterprise-code: DEV_ENTERPRISE_001
    auth-key: dev-auth-key
  scheduler:
    enabled: true
